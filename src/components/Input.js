import React from 'react';export const Input = (props) => {    const {title, id, placeholder, type, value, onChange} = props;    return (        <div className="row">            <div className="col-12 text-center">                <div>{title}</div>                <input                    id={id}                    placeholder={placeholder}                    type={type}                    value={value}                    onChange={onChange}                />            </div>        </div>    )}